#if DEBUG > 0
#include "Debug_prints.h"
#endif

#ifdef BUBBLE
void bubblesort(int *array) {
#if DEBUG > 0
	output(array);
#endif
	for (int i = 0; i <
  LENGTH(array) - 1; ++i){
#ifdef NUMBERS
		int
#else
		char
#endif
		tmp;

	}
}
#endif
